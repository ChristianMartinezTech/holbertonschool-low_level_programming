#include "holberton.h"

/**
 * _isupper - returns 1 for an uppercase character, 0 if its lowercase
 * @c: the character to print
 * Return: Always 0
 */
int _isupper(int c)
{
	if
		(c >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
