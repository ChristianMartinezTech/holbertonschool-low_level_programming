#include "holberton.h"

/**
 * 100-atoi - function that convert a string to an integer.
 * @s: pointer to the value we give the function in main
 * Return (0) if *s is pointing to a NULL byte
 */

int _atoi(char *s)
{
	char *a;

	if (s[0] != NULL)
	{

	}
	else
	{
		return (0)
	}
}